import { createStyles } from "@material-ui/core/styles";

export const styles = (theme) =>
  createStyles({
    paper: {
      maxWidth: "100%",
      margin: "20px 10px 10px 10px",
      overflow: "hidden",
      boxShadow: "none",
      minHeight: "87vh",
    },
  });
